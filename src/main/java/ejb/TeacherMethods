package ejb;

import domain.TeacherDomain;

import javax.ejb.Local;
import java.util.List;

/**
 * Created by Solidbeans on 2017-03-20.
 */
@Local
public interface TeachersMethods {

    void addTeacher(TeacherDomain teacher);
    void updateTeacher(TeacherDomain teacher);
    TeacherDomain getTeacher(Long id);
    void removeTeacher(Long id);
    List<TeachersDomain> getTeachers();
    public List<TeacherDomain> getTeachersFirtsNameContain(String filter) ;
}
